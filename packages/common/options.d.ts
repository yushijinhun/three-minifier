export interface ThreeMinifierOptions {
    /**
     * Resolves three.js additional modules, such as 'three-nodes' and 'three-addons'.
     * Enabled by default.
     */
    resolveAdditionalModules?: boolean;
};
